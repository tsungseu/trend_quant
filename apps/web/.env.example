# apps/web 环境变量示例（复制为 .env / .env.local 后按需修改）

# 「进入终端」按钮指向的终端地址。
# 本地开发：终端 dev server 跑在 5173 端口，history 路由，入口为 /app
#   VITE_TERMINAL_URL=http://localhost:5173/app
# 生产：web 占据站点根（https://tsungseu.github.io/trend_quant/），
#   终端迁至 /terminal/ 子路径，hash 路由入口为
#   /trend_quant/terminal/#/app（apps/web/src/data/terminal.js 默认值已据此推导）
#   如需跨域或自定义部署，显式覆盖：
#   VITE_TERMINAL_URL=https://tsungseu.github.io/trend_quant/terminal/#/app
VITE_TERMINAL_URL=http://localhost:5173/app

# 站点部署 base 路径。默认生产为根（/）；仅当 web 部署到非根子路径时覆盖。
# VITE_PUBLIC_BASE=/
